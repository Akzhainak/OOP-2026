package task1;

public class pr1 {
/*
1. What is the core difference between a class and an interface?
A class can contain fields, constructors, and fully implemented methods, and we can create objects from it.
An interface is a contract that describes what methods a class must provide. It is used to define behavior, and a class implements an interface.

2. Can interfaces have fields? If yes, what are the implicit modifiers for them?
Yes, interfaces can have fields, but they are treated as constants.
All interface fields are implicitly:
public static final
So they must be initialized immediately.

3. Can a class implement multiple interfaces? Explain why this is useful.
Yes, a class can implement multiple interfaces.
This is useful because it allows one class to have several behaviors at the same time. For example, one class can be both Movable and CanHavePizza. It also gives flexibility in design and supports polymorphism.
A bit better version for submission

 */
}
